# 使用系统主题强调色

## 使用
强调色变量作为 css 变量 `--ezi-accent-color` 提供给前端使用。

::: code-group

``` css [css]
/* 使用系统强调色作为按钮背景色 */
.btn {
    background: var(--ezi-accent-color);
}
```

:::

## 配置
在 [窗口选项](/api-reference/window) 中有个参数 `accentColor` 可以控制 `--ezi-accent-color` 的值。  
默认为 `system`，即自动同步系统的强调色。